<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Blog - Mohammad Khurram</title>
  </head>
  <body>
    <h1>Blog Posts</h1>
    <ul>
      {% for post in collections.blog | reverse %}
        <li>
          <a href="{{ post.url }}">{{ post.data.title }}</a>
          <small> - {{ post.date | date('MMMM d, yyyy') }}</small>
        </li>
      {% endfor %}
    </ul>
  </body>
</html>
